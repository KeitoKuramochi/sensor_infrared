.claude/settings.local.json
.claude/.session_markers/
.DS_Store

# Secrets / credentials — never commit these
.env
.env.*
*.pem
*.key
*.p12
*.pfx
*.keystore
.netrc
credentials.json
credentials.yml
credentials.yaml
secrets.json
secrets.yml
secrets.yaml
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
wifi_config.h

# 作業用の元写真・元動画(公開ページ用の縮小版は site/ にコミットする)
# リポジトリ直下に置いた撮影ファイルはコミットしない
/*.jpeg
/*.jpg
/*.MOV
/*.mov

# Python
__pycache__/

# ゲームのランキングデータ(ローカル記録)
/pc_game/ranking.json

# 生成AI画像の元データ(加工済みを pc_game/static/img/ にコミット済み)
/画像/
